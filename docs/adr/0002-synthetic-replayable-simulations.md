# Synthetic, replayable simulations

Volt will model synthetic neighbourhoods only and will not imply meter-backed or settled real-world energy data. Each simulation run is reproducible from its versioned model, frozen inputs, and seed, enabling Monte Carlo sampling without sacrificing inspection or replay.
